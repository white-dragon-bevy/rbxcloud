# Roblox Cloud 环境变量配置示例文件
# 复制此文件为 .env.roblox 或 .env.roblox.local 并根据需要修改

# ============================================
# Roblox Open Cloud API 配置
# ============================================

# Roblox Open Cloud API Key（必需）
# 获取方式: https://create.roblox.com/credentials
#RBXCLOUD_API_KEY=your_api_key_here

# Universe ID（可选，设置后可在命令中省略 --universe-id 参数）
# 示例: RBXCLOUD_UNIVERSE_ID=123456789
#RBXCLOUD_UNIVERSE_ID=

# Place ID（可选，设置后可在命令中省略 --place-id 参数）
# 示例: RBXCLOUD_PLACE_ID=987654321
#RBXCLOUD_PLACE_ID=

# HTTP 代理配置（取消注释以启用）
# HTTP 代理 (用于 http:// 请求)
#HTTP_PROXY=http://proxy.example.com:8080

# HTTPS 代理 (用于 https:// 请求)
#HTTPS_PROXY=http://proxy.example.com:8080

# 全局代理 (同时用于 HTTP 和 HTTPS)
#ALL_PROXY=http://proxy.example.com:8080

# 如果代理需要认证，使用以下格式:
#HTTP_PROXY=http://username:password@proxy.example.com:8080
#HTTPS_PROXY=http://username:password@proxy.example.com:8080

# 不使用代理的主机列表 (逗号分隔)
#NO_PROXY=localhost,127.0.0.1,.example.com

# SOCKS5 代理示例
#ALL_PROXY=socks5://127.0.0.1:1080
